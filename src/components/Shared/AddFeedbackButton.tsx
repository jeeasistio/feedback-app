import { Button } from "../Utils/Button"

export const AddFeedbackButton = () => {
    return (
        <div>
            <Button color="primary" href="/new">
                + Add Feedback
            </Button>
        </div>
    )
}
