import { Typography } from "./Utils/Typography"

export const ReplyButton = () => {
    return (
        <button>
            <Typography variant="body3" color="secondary">Reply</Typography>
        </button>
    )
}
